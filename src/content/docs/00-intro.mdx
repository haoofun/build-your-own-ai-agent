---
title: '00 · 导言：把黑盒拆开'
description: '为什么 agent 没有魔法；最终成品演示；环境准备（Node 22+、API key、约 $5 预算说明）'
---

import Callout from '../../components/Callout'

<Callout type="info" title="正文撰写中">
本章在全书正文完结后最后撰写（成品演示、行数承诺等需以最终代码为准）。以下为章节规划占位。
</Callout>

## 本章规划（占位）

本章不走五段式，职责是「卖出这本书 + 让读者就绪」，计划包含四节：

1. **最终成品演示**：demo GIF（正文完结后录制）。承诺一句话：不到 ___ 行（行数待最终代码定稿后回填），从零手写一个 TUI 编码 agent——读写文件、执行命令、权限确认、上下文自动压缩、子 agent、MCP、skills，零 agent 框架。
2. **这本书怎么教**：每章一个可运行的里程碑、比上一章明显更强；代码按章打 git tag；先撞墙、再讲原理。
3. **一张图当地图**：agent 循环示意图（收集上下文 → 采取行动 → 验证结果），但只作全书路线用——标出每个环节在哪个 Part 完成，不承诺尚未冻结的章号。**刻意不在本章讲循环原理**：为什么需要循环，序章的失忆 CLI 会让你亲身撞上，第一部分再给解药。
4. **开始之前**：Node 22+、API key 获取、约 $5 预算说明（默认教学模型 Haiku）、国内读者指路附录 A。

## 待办

- [ ] 正文完结后：录 demo GIF、回填行数、核对预算承诺
- [ ] 地图图的形态待定（静态 SVG 即可，不占交互孤岛配额）
