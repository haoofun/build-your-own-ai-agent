---
title: '设计样章 · 阅读体验 fixture'
description: '用于设计与验证沉浸式阅读体验的样章——非正式教学内容，不进正式章节'
draft: true
---

import Callout from '../../components/Callout'
import AgentTrace from '../../components/AgentTrace'
import StaticIsland from '../../components/StaticIsland'
import Steps from '../../components/Steps.astro'

<Callout type="info" title="设计 fixture">
这是一个**设计样章**，只为压测阅读排版——散文、代码块、callout、孤岛、列表、引用。**不是正式教学内容**，也不进 sidebar 与大纲。
</Callout>

一句话讲清 agent loop 的形状：每一轮，模型决定调用一个工具，宿主执行它，把结果回灌进 `messages`，循环继续——直到模型不再请求工具。这本书的后半程，全部建立在这个再简单不过的循环之上。

## 为什么需要它

普通的一次 API 调用是「一问一答」：你发 `messages`，拿回一段文本，结束。它没法跑命令、读文件、验证自己的输出。要让模型真正**做事**，你得把它放进一个循环里，并给它一组工具。区别只有一处，却是全部：

- **chat**：一次前向传播，输出文本，到此为止。
- **agent**：同一个模型，外面套一层 loop——执行、观察、再执行。
- 终止条件由模型自己给出：当它不再请求工具，这一轮就是答案。

把这件事写成代码，核心不过十几行：

```ts
// agent loop 的核心：每一轮都重发整个 messages 数组
async function runAgent(messages: Message[]) {
  while (true) {
    const res = await callModel(messages)
    messages.push(res.message)
    if (res.stopReason === 'end_turn') return res // [!code --]
    if (res.stopReason !== 'tool_use') return res // [!code ++]
    const results = await runTools(res.toolCalls)
    messages.push(...results)
  }
}
```

<Callout type="warning" title="一个坑">
`while (true)` 没有上界。真实实现里必须有 `maxTurns` 兜底——否则模型反复请求工具时会无限循环，把你的 API 账单烧穿。
</Callout>

## 原理拆解

「工具」并不神秘：它就是一段 **JSON Schema 声明** + 一个**执行函数**。模型只负责「决定调用哪个、传什么参数」，真正的执行永远在你的代码里。下面这条执行链可以**播放**，也能 ← → **单步**看清每一步是谁发出的——`user` 提问、`assistant` 思考、`tool_use` 调用、`tool_result` 回灌：

<AgentTrace
  client:visible
  title="只读问答"
  events={[
    { role: 'user', text: '读 config.ts，MAX_RETRIES 上限是多少？为什么不能调更高？' },
    { role: 'assistant', text: '只读问答，我读一下 config.ts 就能答。' },
    { role: 'tool_use', name: 'read_file', input: { path: 'src/config.ts' } },
    { role: 'tool_result', exit: 0, text: '// hard-capped at 7 — broker drops messages on the 8th attempt\nexport const MAX_RETRIES = 7;' },
    { role: 'assistant', final: true, text: '上限是 7。再高会触发 broker 在第 8 次投递时关 socket，消息被静默丢弃——注释有据可查。' },
  ]}
/>

注意，整轮里模型从没碰过文件系统——它只是输出了一段「我想调用 `read_file`」的 JSON，是 loop 替它执行、再把结果喂回去的。这条边界，是后面权限系统能成立的根基。

## 动手实现

### read_file 的执行函数

`read_file` 工具的执行函数，朴素到几乎无聊——这正是重点：

```ts
function readFile({ path }: { path: string }): ToolResult {
  const text = fs.readFileSync(path, 'utf8')
  return { ok: true, content: text } // [!code highlight]
}
```

> 工具 = 一段 JSON 声明 + 一个执行函数。模型决定「调用」，执行永远在你这边。把这条记牢，MCP、子 agent、权限确认都只是它的变奏。

同一类孤岛在无 JS / 电子书端会降级成**静态形态**（`StaticIsland`）——全展开、可选中、不依赖交互，但同一条链、同一套角色标注：

<StaticIsland title="静态降级形态" subtitle="ebook / no-JS" caption="电子书端：AgentTrace 降级为 StaticIsland，整条链全展开，文本可选中、可重排。" />

## 跑起来看效果

装配式操作序列用 `Steps` 排版——它只包住一个标准 Markdown 有序列表，序号与轨道由样式画出：

<Steps>
1. **准备环境变量**。把 key 写进当前 shell，`cli.ts` 启动时从 `process.env` 读它，不落盘、不进 git。

   ```bash
   export ANTHROPIC_API_KEY="sk-ant-..."
   ```
2. **让 loop 认识这个工具**。工具声明和执行函数分两处注册，别把它们塞进同一个 switch——第 11 章的子 agent 要复用这张表。

   ```ts
   const TOOLS = [readFileSchema] // [!code ++]
   const HANDLERS = { read_file: readFile } // [!code ++]
   ```
3. **跑一次**，问它一个只能靠读文件回答的问题。终端里应当出现一次 `tool_use`、一次 `tool_result`，然后才是模型的回答。

   ```bash
   npx tsx cli.ts "package.json 里声明了哪些依赖？"
   ```
4. **确认边界**。把 `read_file` 的执行函数临时改成 `throw new Error('boom')`，再跑一遍——agent 应该把错误当成一条普通的工具结果接住，而不是整个进程崩掉。这就是第 10 章「工具错误三段式」的前置直觉。
</Steps>

## 练习与延伸

- 给 loop 加 `maxTurns` 上界，并在到达时返回一条说明而不是静默退出。
- 把 `stopReason` 的判断改成显式白名单，想清楚 `max_tokens` 截断该怎么处理。
- 给只读工具集（`read_file`、`grep`）结构性免审，写操作仍需用户确认。
