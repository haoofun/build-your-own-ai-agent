---
title: 课程大纲
description: 按 Part 规划的能力路线，以及当前暂定的章节展开
---

`// Part 目标 v0.3 · 章节展开持续修订`

从一次 API 调用，到一个几千行的编码 agent。Part 按真实任务需求划分：新能力带来新的工程压力，runtime 再承担新的责任。每个 Part 先固定出口，再从真实代码 checkpoint 反推章节。

> 下方 Part 目标是规划基线；章号、标题和拆分只是当前工作假设。旧 00–16 条目按最接近的 Part 临时归类，横跨新边界的旧 ch10、ch12 仍待重切。

## 序章 · 看见模型边界

**Part 出口**：一个使用裸 fetch 的无状态聊天 CLI；读者能亲手看见失忆，并解释模型调用为什么还不是 agent。

`// 当前章节假设`

- **[`00` 导言：把黑盒拆开](/00-intro)**
  里程碑 → 环境就绪，看完最终成品演示

- **`01` 一次 API 调用**
  里程碑 → 一个会失忆的聊天 CLI——连问"我叫什么"都答不出

## 第一部分 · 核心闭环

**Part 出口**：一个能 Read / Write / Edit / Bash、能把失败测试修到通过的 SDK 版 agent；fetch 与 SDK 行为等价，`runAgent` 可以复用。

`// 当前章节假设`

- **`02` Tool Use：给模型一双手**
  里程碑 → 能查时间、算算术的助手

- **`03` Agent Loop：循环直到完成**
  里程碑 → 记得住你上一句的代码问答 agent

- **`04` 写与改：Write、Edit 与 diff**
  里程碑 → 能修真实 bug 的最小编码 agent

- **`05` Bash：让 agent 跑命令**
  里程碑 → 丢给它一个失败的测试，它自己修到通过

## 第二部分 · 可控运行

**Part 出口**：一个理解项目环境、过程可见、危险操作受控、工具错误可恢复、网络瞬时错误可重试、Ctrl+C 无孤儿进程的 agent。

`// 当前章节假设`

- **`06` 系统提示词与环境感知**
  里程碑 → 注入环境后答对"当前分支有什么未提交改动"

- **`07` 权限系统：信任但确认**
  里程碑 → agent 不再能悄悄 rm -rf

## 第三部分 · 长任务与 Session

**Part 出口**：一个能观测 token、截断大输出、验证 caching、安全 compaction，并用 JSONL 保存和恢复线性 Session 的 agent。

`// 当前章节假设`

- **`08` token 感知：计数、截断与缓存**
  里程碑 → API 账单下降；大输出不再把窗口撑爆

- **`09` 上下文压缩：compaction**
  里程碑 → 长对话不再爆窗口，压缩前后 usage 眼见为实

- **`10` 健壮性与会话生命周期**
  里程碑 → 429 退避、Ctrl+C 优雅取消、关终端重开对话还在

## 第四部分 · 计划与分工

**Part 出口**：Todo 跨 prompt 保存进度；只读子 agent 复用 `runAgent`，返回可复查证据，并完整上卷 usage 与取消。

`// 当前章节假设`

- **`11` 子 agent：分而治之**
  里程碑 → 主 agent 派只读子 agent 全库搜索，自己保持清爽

- **`12` 计划、命令与 Skills**
  里程碑 → 开 todo 不漏步骤；/commit skill 按需加载眼见为实

> 旧 ch12 同时包含 Todo 和 Skills，进入第四、第五部分开发时会拆开重排；Commands 在真实需求出现时分别加入。

## 第五部分 · 开放扩展

**Part 出口**：Skills 按需进入 system prompt；MCP 工具经过命名、权限和错误适配后进入同一个 Tool Map。

`// 当前章节假设`

- **`13` MCP：接入外部世界**
  里程碑 → 你的 agent 能用上整个 MCP 生态

## 第六部分 · 验证与交付

**Part 出口**：终端体验完整，eval 产出确定性报告，能力证据分级，干净环境可以在三分钟内安装跑通。

`// 当前章节假设`

- **`14` 终端体验打磨**
  里程碑 → 流式 markdown、spinner、工具调用折叠

- **`15` 评测：怎么知道它变好了**
  里程碑 → 三配置的通过率与 token 成本对比表

- **`16` 发布**
  里程碑 → 干净容器里 npm i -g 后三分钟跑通 demo

## 附录

- **`A` API 提供商选择**
  Anthropic 直连 / OpenRouter / 任意 OpenAI 兼容端点

- **`B` 多模型适配层**
  在不同模型供应商之间切换的适配层设计

- **`C` 术语表（中英对照）**
  全书技术名词的中英文对照表
