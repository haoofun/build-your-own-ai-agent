# 写作规范（单源三端的兼容约定）

`book/*.md` 是唯一内容源，同时供给：VitePress 网站、pandoc 电子书（epub/PDF）、llms.txt（agent 可读端）。
本规范的每条都为了"写一份、三端不坏"。评审章节时按此检查。

## 文件与结构

- 文件名：`NN-slug.md`（如 `03-agent-loop.md`），**不含空格**；章节顺序 = 文件名排序（电子书脚本依赖此约定）
- 附录：`appendix-a-*.md` 等（字母排在数字后，自然落在书尾）
- 每章**有且仅有一个 H1**（章标题），正文从 H2 开始
- 新章上线：把文件放进 `book/` 即可，侧边栏按 `.vitepress/config.ts` 中的规划目录自动出现，无需改配置
- 五段式结构（H2）：为什么需要它 → 原理拆解 → 动手实现 → 跑起来看效果 → 练习与延伸

## 图片

- 一律相对路径：`![描述](./images/xxx.png)`；不用 HTML `<img>`（pandoc 端语义丢失）
- 图片文件名不含空格；放 `book/images/`

## 交互孤岛（唯一合法写法）

```markdown
::: {.interactive name="agent-loop-visualizer"}
![agent loop 静态示意图](./images/agent-loop.png)
:::
```

- 网站端：渲染为交互容器（组件按 `name` 挂载，M1 后实现；现阶段显示降级图）
- 电子书端：pandoc 的 fenced_divs 识别花括号写法，自动降级为内部静态图
- **实验证据（2026-06-10）**：裸词写法 `::: interactive xxx` pandoc **不识别**，`:::` 会作为正文漏进电子书——禁止使用
- 全书交互点控制在关键处（三五个），每个必须配静态降级图

## 语法红线

- 禁 MDX / JSX / Vue SFC 语法混入 markdown
- 正文中的 `{{ }}` 会被 VitePress 当 Vue 插值解析：代码块内安全；行内文本需用反引号包裹
- VitePress 专属容器（`::: tip` 等）可用——pandoc 会解析为无样式 div，内容不丢，但电子书端无视觉效果，慎用于关键信息
- 章间互链用相对路径：`[上一章](./02-tool-use.md)`（VitePress 自动转 html 链接；电子书跨章锚点 M4 校对时统一核验）
- 代码块标注语言（```ts、```bash），三端高亮/语义都依赖它

## 章节模板

```markdown
# NN · 章标题

一段引子：上一章结束时项目能做什么、还不能做什么。

## 为什么需要它

## 原理拆解

## 动手实现

## 跑起来看效果

## 练习与延伸
```
